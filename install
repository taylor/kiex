#!/usr/bin/env bash

mkdir -p $HOME/bin
curl -qs -o $HOME/bin/kiex https://raw.github.com/taylor/kiex/master/kiex
chmod +x $HOME/bin/kiex

echo "kiex has been installed in $HOME/bin.  Make sure that directory is in your path"
echo "Type kiex to start"
